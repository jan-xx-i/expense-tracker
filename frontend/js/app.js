const API_BASE='http://localhost:8080/api';
const CATEGORIES=['FOOD','TRANSPORT','SHOPPING','BILLS','EDUCATION','ENTERTAINMENT','HEALTH','OTHER'];
const PAYMENT_METHODS=['Cash','Card','UPI','Net Banking','Other'];
const state={user:JSON.parse(localStorage.getItem('user')||'null'),route:route(),error:''};
function route(){const h=location.hash.replace(/^#/,'')||'/';return h.split('?')[0]||'/'}
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function currency(v){return '₹'+(Number(v)||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})}
function dateFmt(v){if(!v)return '';return new Date(v).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}
function titleCase(s){return s?s.charAt(0)+s.slice(1).toLowerCase():''}
function today(){return new Date().toISOString().slice(0,10)}
async function api(path,opts={}){const headers={'Content-Type':'application/json',...(opts.headers||{})};const token=localStorage.getItem('token');if(token)headers.Authorization='Bearer '+token;const r=await fetch(API_BASE+path,{...opts,headers});if(r.status===401){localStorage.removeItem('token');localStorage.removeItem('user');state.user=null;location.hash='#/login';throw new Error('Unauthorized')}let data=null;try{data=await r.json()}catch{}if(!r.ok){const e=new Error(data?.error||data?.message||'Request failed');e.data=data;throw e}return data}
function navigate(p){location.hash='#'+p}
function logout(){localStorage.removeItem('token');localStorage.removeItem('user');state.user=null;navigate('/login')}
function layout(content,active){return `<div class="app-layout"><aside class="sidebar"><div class="sidebar-brand">Expense<span>Track</span></div><a class="sidebar-link ${active==='/'?'active':''}" href="#/">Dashboard</a><a class="sidebar-link ${active==='/expenses'?'active':''}" href="#/expenses">Expenses</a><a class="sidebar-link ${active==='/income'?'active':''}" href="#/income">Income</a><button class="sidebar-logout" id="logout" title="${esc(state.user?.email)}">Log out ${state.user?.name?'('+esc(state.user.name)+')':''}</button></aside><main class="main-content">${content}</main></div>`}
function alertBox(msg){return msg?`<div class="alert alert-error">${esc(msg)}</div>`:''}
function loading(){return '<div class="loading-spinner">Loading...</div>'}
function empty(msg){return `<div class="empty-state">${esc(msg)}</div>`}
function authPage(mode){const login=mode==='login';return `<div class="auth-page"><div class="card auth-card"><h1>${login?'Welcome back':'Create your account'}</h1><p class="subtitle">${login?'Log in to manage your expenses':'Start tracking your income and expenses'}</p><div id="auth-error"></div><form id="auth-form">${!login?'<div class="form-group"><label>Full Name</label><input name="name" required></div>':''}<div class="form-group"><label>Email</label><input name="email" type="email" required></div><div class="form-group"><label>Password</label><input name="password" type="password" required ${!login?'minlength="6"':''}></div><button class="btn btn-primary" style="width:100%" id="auth-submit">${login?'Log In':'Register'}</button></form><div class="switch-link">${login?"Don't have an account? ":'Already have an account? '}<a href="#/${login?'register':'login'}">${login?'Register':'Log In'}</a></div></div></div>`}
async function renderAuth(login){document.getElementById('app').innerHTML=authPage(login?'login':'register');document.getElementById('auth-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),data=Object.fromEntries(f.entries());const er=document.getElementById('auth-error'),btn=document.getElementById('auth-submit');if(!login&&data.password.length<6){er.innerHTML=alertBox('Password must be at least 6 characters');return}btn.disabled=true;btn.textContent=login?'Logging in...':'Creating account...';try{const r=await api(login?'/auth/login':'/auth/register',{method:'POST',body:JSON.stringify(data)});localStorage.setItem('token',r.token);const u={userId:r.userId,name:r.name,email:r.email};localStorage.setItem('user',JSON.stringify(u));state.user=u;navigate('/')}catch(e){er.innerHTML=alertBox(e.message||'Request failed')}finally{btn.disabled=false;btn.textContent=login?'Log In':'Register'}}}
function modal(title,body){return `<div class="modal-overlay" id="modal"><div class="modal-box" onclick="event.stopPropagation()"><h2>${title}</h2>${body}</div></div>`}
function expenseForm(initial){const x=initial||{title:'',amount:'',category:'FOOD',description:'',date:today(),paymentMethod:'Cash'};return `<form id="expense-form"><div class="form-group"><label>Title</label><input name="title" value="${esc(x.title)}" placeholder="e.g. Groceries"><div class="form-error" id="err-title"></div></div><div class="form-group"><label>Amount (₹)</label><input name="amount" type="number" step="0.01" value="${esc(x.amount)}" placeholder="0.00"><div class="form-error" id="err-amount"></div></div><div class="form-group"><label>Category</label><select name="category">${CATEGORIES.map(c=>`<option value="${c}" ${x.category===c?'selected':''}>${titleCase(c)}</option>`).join('')}</select></div><div class="form-group"><label>Date</label><input name="date" type="date" value="${esc(x.date)}"><div class="form-error" id="err-date"></div></div><div class="form-group"><label>Payment Method</label><select name="paymentMethod">${PAYMENT_METHODS.map(p=>`<option ${x.paymentMethod===p?'selected':''}>${p}</option>`).join('')}</select></div><div class="form-group"><label>Description (optional)</label><textarea name="description" rows="2">${esc(x.description)}</textarea></div><div class="modal-actions"><button type="button" class="btn btn-secondary" id="modal-cancel">Cancel</button><button class="btn btn-primary" id="save-btn">Save Expense</button></div></form>`}
function incomeForm(initial){const x=initial||{source:'',amount:'',description:'',date:today()};return `<form id="income-form"><div class="form-group"><label>Source</label><input name="source" value="${esc(x.source)}" placeholder="e.g. Salary, Freelance"><div class="form-error" id="err-source"></div></div><div class="form-group"><label>Amount (₹)</label><input name="amount" type="number" step="0.01" value="${esc(x.amount)}" placeholder="0.00"><div class="form-error" id="err-amount"></div></div><div class="form-group"><label>Date</label><input name="date" type="date" value="${esc(x.date)}"><div class="form-error" id="err-date"></div></div><div class="form-group"><label>Description (optional)</label><textarea name="description" rows="2">${esc(x.description)}</textarea></div><div class="modal-actions"><button type="button" class="btn btn-secondary" id="modal-cancel">Cancel</button><button class="btn btn-primary" id="save-btn">Save Income</button></div></form>`}
function showModal(title,body){document.body.insertAdjacentHTML('beforeend',modal(title,body));const m=document.getElementById('modal');m.onclick=()=>m.remove();document.getElementById('modal-cancel').onclick=()=>m.remove()}
async function dashboard(){const el=document.getElementById('app');el.innerHTML=layout(`<div class="page-header"><h1>Dashboard</h1></div><div id="dash">${loading()}</div>`,'/');try{const d=await api('/dashboard');document.getElementById('dash').innerHTML=`<div class="summary-grid"><div class="card summary-card balance"><div class="label">Total Balance</div><div class="value">${currency(d.totalBalance)}</div></div><div class="card summary-card income"><div class="label">Total Income</div><div class="value">${currency(d.totalIncome)}</div></div><div class="card summary-card expense"><div class="label">Total Expenses</div><div class="value">${currency(d.totalExpense)}</div></div><div class="card summary-card expense"><div class="label">This Month's Expenses</div><div class="value">${currency(d.currentMonthExpense)}</div></div></div><div class="charts-grid"><div class="card"><h3 style="margin-top:0">Income vs Expense (last 6 months)</h3><canvas class="chart-canvas" id="trend"></canvas><div class="chart-legend"><span><i class="legend-dot" style="background:#16a34a"></i>Income</span><span><i class="legend-dot" style="background:#dc2626"></i>Expense</span></div></div><div class="card"><h3 style="margin-top:0">Spending by Category</h3><div class="pie-wrap"><canvas class="pie-canvas" id="pie"></canvas><div id="pie-legend" class="chart-legend"></div></div></div></div><div class="card"><h3 style="margin-top:0">Recent Transactions</h3>${d.recentExpenses?.length?`<div class="table-wrapper"><table><thead><tr><th>Title</th><th>Category</th><th>Date</th><th>Amount</th></tr></thead><tbody>${d.recentExpenses.map(e=>`<tr><td>${esc(e.title)}</td><td><span class="badge">${titleCase(e.category)}</span></td><td>${dateFmt(e.date)}</td><td>${currency(e.amount)}</td></tr>`).join('')}</tbody></table></div>`:empty('No expenses recorded yet. Add your first one from the Expenses page.')}</div>`;drawBar(d.monthlyTrend||[]);drawPie(d.categoryBreakdown||{})}catch(e){document.getElementById('dash').innerHTML=alertBox('Could not load dashboard data.')}}
function resizeCanvas(c){const r=c.getBoundingClientRect(),d=devicePixelRatio||1;c.width=r.width*d;c.height=r.height*d;const x=c.getContext('2d');x.scale(d,d);return [x,r.width,r.height]}
function drawBar(data){const c=document.getElementById('trend');if(!c)return;if(!data.length){c.parentElement.innerHTML+=empty('No trend data yet.');return}const [x,w,h]=resizeCanvas(c);const max=Math.max(1,...data.map(d=>Math.max(Number(d.income)||0,Number(d.expense)||0)));const left=38,bottom=28,top=10,plotH=h-top-bottom,slot=(w-left-10)/data.length,barW=Math.min(16,slot/3);x.font='12px Segoe UI';x.strokeStyle='#e5e9f0';x.fillStyle='#6b7280';x.textAlign='center';for(let i=0;i<=4;i++){const y=top+plotH*i/4;x.beginPath();x.moveTo(left,y);x.lineTo(w,y);x.stroke();}data.forEach((d,i)=>{const cx=left+slot*i+slot/2,iv=Number(d.income)||0,ev=Number(d.expense)||0,ih=iv/max*plotH,eh=ev/max*plotH;x.fillStyle='#16a34a';x.fillRect(cx-barW-2,top+plotH-ih,barW,ih);x.fillStyle='#dc2626';x.fillRect(cx+2,top+plotH-eh,barW,eh);x.fillStyle='#6b7280';x.fillText(d.month,cx,h-8)})}
function drawPie(data){const c=document.getElementById('pie'),leg=document.getElementById('pie-legend');if(!c)return;const arr=Object.entries(data).filter(([,v])=>Number(v)>0).map(([k,v])=>[titleCase(k),Number(v)]);if(!arr.length){c.parentElement.innerHTML=empty('No expenses yet to chart.');return}const colors=['#4f46e5','#16a34a','#d97706','#dc2626','#0891b2','#9333ea','#db2777','#64748b'];const [x,w,h]=resizeCanvas(c),cx=w/2,cy=h/2,r=Math.min(w,h)*.34,total=arr.reduce((s,a)=>s+a[1],0);let a=-Math.PI/2;arr.forEach((v,i)=>{const end=a+v[1]/total*Math.PI*2;x.beginPath();x.moveTo(cx,cy);x.arc(cx,cy,r,a,end);x.closePath();x.fillStyle=colors[i%colors.length];x.fill();a=end});leg.innerHTML=arr.map((v,i)=>`<span><i class="legend-dot" style="background:${colors[i%colors.length]}"></i>${esc(v[0])}</span>`).join('')}
async function expenses(){const el=document.getElementById('app');el.innerHTML=layout(`<div class="page-header"><h1>Expenses</h1><button class="btn btn-primary" id="add">+ Add Expense</button></div><div id="error"></div><div class="toolbar"><input id="search" placeholder="Search by title..."><select id="category"><option value="">All Categories</option>${CATEGORIES.map(c=>`<option value="${c}">${titleCase(c)}</option>`).join('')}</select><select id="sort"><option value="date">Sort by Date</option><option value="amount">Sort by Amount</option></select><select id="direction"><option value="desc">Descending</option><option value="asc">Ascending</option></select></div><div class="card" id="expense-table">${loading()}</div>`,'/expenses');let rows=[];const load=async()=>{document.getElementById('expense-table').innerHTML=loading();try{const p=new URLSearchParams({sortBy:document.getElementById('sort').value,direction:document.getElementById('direction').value});if(document.getElementById('category').value)p.set('category',document.getElementById('category').value);rows=await api('/expenses?'+p);renderExpenseRows()}catch(e){document.getElementById('error').innerHTML=alertBox('Could not load expenses.')}};const renderExpenseRows=()=>{const q=document.getElementById('search').value.toLowerCase(),v=rows.filter(e=>e.title.toLowerCase().includes(q));document.getElementById('expense-table').innerHTML=v.length?`<div class="table-wrapper"><table><thead><tr><th>Title</th><th>Category</th><th>Date</th><th>Payment</th><th>Amount</th><th></th></tr></thead><tbody>${v.map(e=>`<tr><td>${esc(e.title)}</td><td><span class="badge">${titleCase(e.category)}</span></td><td>${dateFmt(e.date)}</td><td>${esc(e.paymentMethod||'-')}</td><td>${currency(e.amount)}</td><td style="white-space:nowrap"><button class="btn btn-secondary btn-sm edit" data-id="${e.id}" style="margin-right:6px">Edit</button><button class="btn btn-danger btn-sm del" data-id="${e.id}">Delete</button></td></tr>`).join('')}</tbody></table></div>`:empty('No expenses found. Try adjusting your filters or add a new expense.');document.querySelectorAll('.edit').forEach(b=>b.onclick=()=>openExpense(rows.find(x=>String(x.id)===b.dataset.id)));document.querySelectorAll('.del').forEach(b=>b.onclick=()=>confirmDelete('Expense',rows.find(x=>String(x.id)===b.dataset.id),async x=>{await api('/expenses/'+x.id,{method:'DELETE'});load()}))};document.getElementById('add').onclick=()=>openExpense(null,load);['category','sort','direction'].forEach(id=>document.getElementById(id).onchange=load);document.getElementById('search').oninput=renderExpenseRows;await load()}
function openExpense(initial,reload){showModal(initial?'Edit Expense':'Add Expense',expenseForm(initial));const form=document.getElementById('expense-form');form.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form).entries()),errs={};if(!d.title.trim())errs.title='Title is required';if(!d.amount||Number(d.amount)<=0)errs.amount='Amount must be greater than 0';if(!d.date)errs.date='Date is required';Object.keys(errs).forEach(k=>document.getElementById('err-'+k).textContent=errs[k]);if(Object.keys(errs).length)return;const btn=document.getElementById('save-btn');btn.disabled=true;btn.textContent='Saving...';try{const body={...d,amount:Number(d.amount)};await api(initial?'/expenses/'+initial.id:'/expenses',{method:initial?'PUT':'POST',body:JSON.stringify(body)});document.getElementById('modal').remove();if(reload)reload();else navigate('/expenses')}catch(e){alert(e.message)}finally{btn.disabled=false;btn.textContent='Save Expense'}}}
async function income(){const el=document.getElementById('app');el.innerHTML=layout(`<div class="page-header"><h1>Income</h1><button class="btn btn-primary" id="add">+ Add Income</button></div><div id="error"></div><div class="toolbar"><input id="search" placeholder="Search by source..."></div><div class="card" id="income-table">${loading()}</div>`,'/income');let rows=[];const load=async()=>{document.getElementById('income-table').innerHTML=loading();try{rows=await api('/income');render()}catch(e){document.getElementById('error').innerHTML=alertBox('Could not load income records.')}};const render=()=>{const q=document.getElementById('search').value.toLowerCase(),v=rows.filter(i=>i.source.toLowerCase().includes(q));document.getElementById('income-table').innerHTML=v.length?`<div class="table-wrapper"><table><thead><tr><th>Source</th><th>Date</th><th>Description</th><th>Amount</th><th></th></tr></thead><tbody>${v.map(i=>`<tr><td>${esc(i.source)}</td><td>${dateFmt(i.date)}</td><td>${esc(i.description||'-')}</td><td>${currency(i.amount)}</td><td style="white-space:nowrap"><button class="btn btn-secondary btn-sm edit" data-id="${i.id}" style="margin-right:6px">Edit</button><button class="btn btn-danger btn-sm del" data-id="${i.id}">Delete</button></td></tr>`).join('')}</tbody></table></div>`:empty('No income records found. Add your first one.');document.querySelectorAll('.edit').forEach(b=>b.onclick=()=>openIncome(rows.find(x=>String(x.id)===b.dataset.id)));document.querySelectorAll('.del').forEach(b=>b.onclick=()=>confirmDelete('Income',rows.find(x=>String(x.id)===b.dataset.id),async x=>{await api('/income/'+x.id,{method:'DELETE'});load()}))};document.getElementById('add').onclick=()=>openIncome(null,load);document.getElementById('search').oninput=render;await load()}
function openIncome(initial,reload){showModal(initial?'Edit Income':'Add Income',incomeForm(initial));const form=document.getElementById('income-form');form.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form).entries()),errs={};if(!d.source.trim())errs.source='Source is required';if(!d.amount||Number(d.amount)<=0)errs.amount='Amount must be greater than 0';if(!d.date)errs.date='Date is required';Object.keys(errs).forEach(k=>document.getElementById('err-'+k).textContent=errs[k]);if(Object.keys(errs).length)return;const btn=document.getElementById('save-btn');btn.disabled=true;btn.textContent='Saving...';try{const body={...d,amount:Number(d.amount)};await api(initial?'/income/'+initial.id:'/income',{method:initial?'PUT':'POST',body:JSON.stringify(body)});document.getElementById('modal').remove();if(reload)reload();else navigate('/income')}catch(e){alert(e.message)}finally{btn.disabled=false;btn.textContent='Save Income'}}}
function confirmDelete(type,item,action){showModal('Delete '+type,`<p>Delete "${esc(type==='Expense'?item.title:item.source)}"? This cannot be undone.</p><div class="modal-actions"><button class="btn btn-secondary" id="modal-cancel">Cancel</button><button class="btn btn-danger" id="confirm-delete">Delete</button></div>`);document.getElementById('confirm-delete').onclick=async()=>{const b=document.getElementById('confirm-delete');b.disabled=true;b.textContent='Deleting...';try{await action(item);document.getElementById('modal').remove()}catch(e){alert(e.message)}finally{b.disabled=false;b.textContent='Delete'}}}
async function render(){state.route=route();if(state.route!=='/login'&&state.route!=='/register'&&!state.user){return navigate('/login')}if((state.route==='/login'||state.route==='/register')&&state.user)return navigate('/');if(state.route==='/login')return renderAuth(true);if(state.route==='/register')return renderAuth(false);if(state.route==='/')return dashboard();if(state.route==='/expenses')return expenses();if(state.route==='/income')return income();navigate('/')}
window.addEventListener('hashchange',render);render();
